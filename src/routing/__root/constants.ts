export const ERRORS = {
  stateProviderHookMissingContext:
    "useRootLayoutState must be used within Provider",
  actionProviderHookMissingContext:
    "useRootLayoutActions must be used within Provider",
} as const;
